# 🎵 BeatSage Auto Uploader

Автоматизированный инструмент для конвертации MP3-песен в уровни для Beat Saber через сервис BeatSage.com

## 📋 Оглавление
- [Возможности](#-возможности)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)

## Возможности

**Автоматическая загрузка** — загружает все MP3-файлы из папки `songs` на BeatSage.com  
**Автоматическое заполнение форм** — отмечает все чекбоксы на странице  
**Пакетная обработка** — обрабатывает все песни за один запуск  
**Распаковка архивов** — автоматическая распаковка и перемещение уровней в папку игры  
**Кроссплатформенность** — работает на Windows, macOS и Linux  

## Установка

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Установка драйвера браузера
- **Chrome**: Установите браузер Google Chrome

## ⚙️ Настройка

### 📁 Настройка файла `config.py`

Откройте файл `config.py` и настройте следующие параметры:

```python
# Обработка песен
SONGS_FOLDER = "songs"  # Папка с MP3-файлами (не меняйте, если используете стандартную структуру)

URL_PATH = "https://beatsage.com/"  # URL BeatSage (обычно не требует изменений)

# Распаковка
# Если нужно распаковать песни и сразу перенести в папку с игрой установите для переменной NEED_TO_UNPACK значение True
NEED_TO_UNPACK = False  # Автоматическая распаковка при запуске (True/False)

INPUT_PATH = "~/Downloads"  # Путь к папке загрузок (где скачиваются архивы)

UNPACKED_PATH = r"absolute_path"  # ⚠️ ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ!
# Укажите абсолютный путь к папке с пользовательскими уровнями Beat Saber:
```

### 🔧 Рекомендуемые настройки:

1. **Для первого запуска**:
   ```python
   NEED_TO_UNPACK = False
   ```
   После загрузки всех песен программа спросит, хотите ли вы распаковать архивы.

2. **Для автоматической работы**:
   ```python
   NEED_TO_UNPACK = True
   ```
   Программа автоматически распакует архивы после загрузки.

## Использование

### Подготовка песен
1. Поместите все MP3-файлы в папку `songs/`
   ```
   beatsage-auto-uploader/
   ├── songs/
   │   ├── песня1.mp3
   │   ├── песня2.mp3
   │   └── ...
   ```

### 🖥️ Запуск программы
```bash
python main.py
```

### Процесс преобразования

1. **Автоматическое открытие браузера** — программа откроет Chrome
2. **Последовательная загрузка** — каждый MP3-файл будет загружен в отдельной вкладке
3. **Автозаполнение формы** — все чекбоксы будут автоматически отмечены
4. **Генерация** — подождите, пока все архивы с уровнями скачаются, после чего подтвердите это см. п. 5
5. **Ожидание** — программа ждет, пока вы нажмете Enter
6. **Скачивание** — вы вручную скачиваете сгенерированные архивы в папку загрузок
7. **Распаковка** — программа распаковывает архивы в папку с игрой (при включенной опции)

## Структура проекта

```
beatsage-auto-uploader/
├── songs/               # Папка с исходными MP3-файлами
├── config.py           # Файл конфигурации
├── main.py            # Основной скрипт
├── unpack.py          # Скрипт распаковки архивов
├── requirements.txt   # Зависимости Python
└── README.md         # Эта документация
```

## 🤝 Вклад в проект

Если вы нашли ошибку или хотите предложить улучшение:
1. Создайте Issue с описанием проблемы
2. Предложите Pull Request с исправлением
3. Опишите шаги для воспроизведения ошибки


**Примечание**: Этот инструмент предназначен для личного использования

**Удачной игры в Beat Saber!**